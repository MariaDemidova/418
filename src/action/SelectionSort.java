package action;
/*
import java.util.Arrays;

public static void sort1 (String[] args) {
    String[] array = {"B","F", "FL", "M", "K", "BC", "L", "A"};
    // сортировка выбором по возрастанию
    selectionSort1(array);
    // вывод
    System.out.println(Arrays.toString(array));
}
public static void selectionSort1(String[] array) {
    // обход всех подмножеств элементов массива
    // (1-последний, 2-последний, 3-последний, ...)
    for (int i = 0; i < array.length - 1; i++) {
        // установить текущий элемент как минимум
        int min = i;
        // обход последующих элементов
        // чтобы найти наименьший
        for (int j = i + 1; j < array.length; j++) {
            // если этот элемент меньше,
            // тогда это новый минимум
            if (array[j].compareTo(array[min]) < 0) {
                min = j;
            }
        }
        // если максимальный элемент не равен
        // текущему, тогда поменять их местами
        if (min != i) {
            String temp = array[i];
            array[i] = array[min];
            array[min] = temp;
        }
    }
}
*/






































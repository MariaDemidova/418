package action;
/*
import console.ConsoleUI;
import generator.Generator;
import java.util.Map;

public class Sort {
    private final Generator generator;

    public Sort(Generator generator) {
        this.generator = generator;
    }

    private void sort() {
        SelectionSort.sort(generator.buses);
        SelectionSort.sort(generator.users);
        SelectionSort.sort(generator.students);
    }

    @Override
    public String doing(Map<String,String> params) {
        this.sort();
        return "Коллекция отсортирована";
    }
}
*/